import React from 'react';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './presentation.module.css';

export default function PosterPage() {
  const posterUrl = useBaseUrl('img/Poster - PEI.png');

  return (
    <div className={styles.pdfContainer} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', height: '80vh' }}>
      <div style={{ marginBottom: '15px' }}>
        <a 
          href={posterUrl} 
          target="_blank" 
          rel="noopener noreferrer"
          style={{
            backgroundColor: '#0070f3',
            color: 'white',
            padding: '8px 16px',
            borderRadius: '4px',
            textDecoration: 'none',
            fontWeight: 'bold',
            fontSize: '14px',
            boxShadow: '0 2px 5px rgba(0,0,0,0.2)',
            transition: 'background-color 0.2s'
          }}
          onMouseOver={(e) => e.currentTarget.style.backgroundColor = '#0051a8'}
          onMouseOut={(e) => e.currentTarget.style.backgroundColor = '#0070f3'}
        >
          View Full Size / Download Poster
        </a>
      </div>
      <div style={{ 
        flex: 1, 
        width: '100%', 
        display: 'flex', 
        justifyContent: 'center', 
        alignItems: 'center', 
        overflow: 'hidden',
        border: '1px solid #ddd',
        borderRadius: '8px',
        backgroundColor: '#f5f5f5'
      }}>
        <img
          src={posterUrl}
          alt="Project Academic Poster"
          style={{
            maxWidth: '100%',
            maxHeight: '100%',
            objectFit: 'contain',
            boxShadow: '0 4px 15px rgba(0,0,0,0.1)'
          }}
        />
      </div>
    </div>
  );
}
